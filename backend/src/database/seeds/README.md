# Seeds

Place seed scripts and utilities in this directory.

Example seed script entrypoint: `src/database/seeds/seed.ts`.

Run with:

```
npm run ts-node src/database/seeds/seed.ts
```

Or add an npm script for convenience.
import React from 'react';
import Icon from '../components/Icon';

export default function Building2({ size = 24, color, style }) {
  return <Icon name="business" size={size} color={color} style={style} />;
}
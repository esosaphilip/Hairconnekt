import React from 'react';
import Icon from '../components/Icon';

export default function ChevronRightIcon({ size = 24, color, style }) {
  return <Icon name="chevron-forward" size={size} color={color} style={style} />;
}